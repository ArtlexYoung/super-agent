schema_version = 3
name = "default"
type = "memory"
description = "Built-in temporary, long-term, and explicit promotion behavior"
version = "0.0.64"
triggers = []

[configuration]
default_scope = "agent"
recall_limit = 20
include_in_prompt = true
include_usage_habits = true
organize_on_recall = true
