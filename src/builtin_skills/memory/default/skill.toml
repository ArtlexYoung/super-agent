schema_version = 2
name = "default"
capability = "memory"
description = "Built-in event-based memory"
version = "0.0.31"
triggers = []

[configuration]
default_scope = "agent"
recall_limit = 20
include_in_prompt = true
include_usage_habits = true
