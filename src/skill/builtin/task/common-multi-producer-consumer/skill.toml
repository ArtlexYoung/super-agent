type = "task"
description = "Coordinate general multi-Agent work through native queues and event-driven completion"

[configuration]
mode = "loop"
max_steps = 20

[configuration.tools.agent_tasks]
max_tasks = 32
max_wait_seconds = 60
