Improve the selected Skill against the recorded evidence without weakening behavior that already passes.

Address every reported signal, preserve the Skill format and public contract, and prefer a smaller, clearer change. A candidate may be promoted only when every evaluation case passes, the configured minimum score is reached, and no matching baseline case regresses. Monitor promoted revisions and roll back a measured regression explicitly.
