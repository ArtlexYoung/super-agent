schema_version = 3
name = "code"
type = "workflow"
description = "Iterative repository tool loop with explicit verification and completion"
version = "0.0.101"
agent_created = false
agent_can_update = false
freshness = 70
function_group = "code-workflow"
provides = ["code-workflow"]
requires = []
default = false

[configuration]
mode = "loop"
max_steps = 20

[entry]
instructions = "SKILL.md"
