schema_version = 3
name = "stateless"
type = "scene"
description = "Storage-free task scene with prompt guidance, planning, and direct execution"
version = "0.0.68"
triggers = []
agent_created = false
agent_can_update = false
freshness = 70
function_group = "scene-selection"
provides = ["stateless-scene"]
requires = []
default = false

[configuration]
skills = [
  "prompt:common",
  "planner:default",
  "workflow:direct",
]
