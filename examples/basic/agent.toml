[agent]
name = "basic"
system = "You are a concise, helpful agent."
workflow = "direct"
memory = "default"
skills = ["echo"]
use_features = ["skill"]
disable_names = []

[paths]
skills = ["skills"]

[storage]
backend = "jsonl"
path = ".super-agent"
