[agent]
name = "basic"
system = "You are a concise, helpful agent."
workflow = "direct"
skills = ["echo"]
use_features = ["skill", "memory", "mcp"]
disable_names = []

[model]
provider = "mock"
model = "mock"

[paths]
skills = ["skills"]
mcp = ["mcp"]
memory = ".super-agent/memory"
