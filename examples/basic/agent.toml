[agent]
name = "basic"
system = "You are a concise, helpful agent."
workflow = "direct"
memory = "default"
skills = ["echo"]
use_features = ["skill"]
disable_names = []

[model]
provider = "mock"
model = "mock"

[paths]
skills = ["skills"]

[storage]
backend = "jsonl"
path = ".super-agent"
