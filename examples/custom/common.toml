version = 1
name = "custom-example"
instructions = ["Use the selected task Skill."]
skill_paths = ["skills"]
models = [{ name = "offline", provider = "mock", model = "The custom Skill was selected." }]
