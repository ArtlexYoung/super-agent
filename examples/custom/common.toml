version = 1
name = "custom-example"
instructions = ["Use the selected task Skill."]
library_paths = ["."]
enabled_plugins = ["plugin:local/custom"]
models = [{ name = "offline", provider = "mock", model = "The custom Skill was selected.", description = "Offline model for the custom Skill example" }]
