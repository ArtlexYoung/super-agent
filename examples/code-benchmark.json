{
  "schema_version": 2,
  "agents": [
    {
      "name": "super-agent",
      "version": "0.1.13",
      "command": [
        "{python}",
        "{project_root}/src/cli.py",
        "--output",
        "json",
        "{prompt}"
      ],
      "environment": {
        "PYTHONDONTWRITEBYTECODE": "1",
        "SUPER_AGENT_PROVIDER": "openai-compatible",
        "SUPER_AGENT_MODEL": "THUDM/GLM-4-9B-0414",
        "SUPER_AGENT_BASE_URL": "https://api.siliconflow.cn/v1",
        "SUPER_AGENT_API_KEY_ENV": "OA3_SILICONFLOW_API_KEY"
      },
      "result_json_field": "text"
    }
  ],
  "tasks": [
    {
      "id": "review-zero-division",
      "prompt": "Review this Python function and state the concrete bug: def average(total, count): return total / count",
      "workspace": null,
      "checks": {
        "output_contains": ["division by zero"],
        "output_excludes": [],
        "files": []
      }
    },
    {
      "id": "repair-zero-division",
      "prompt": "Return a one-line safe Python implementation for average(total, count). Include exactly this expression: return total / count if count else 0",
      "workspace": null,
      "checks": {
        "output_contains": ["return total / count if count else 0"],
        "output_excludes": [],
        "files": []
      }
    }
  ]
}
