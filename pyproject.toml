[project]
name = "super-agent"
version = "0.0.4"
description = "A lightweight, configurable, skill-first agent runtime."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Super Agent contributors" }]
dependencies = []

[project.scripts]
super-agent = "super_agent.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
